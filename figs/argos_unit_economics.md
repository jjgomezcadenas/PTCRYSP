# ARGOS unit economics

The maintained model is now in [../costs/README.md](../costs/README.md).

Edit the four CSV source tables there, then use `build_costs.py` to regenerate the Excel workbook and financial slides. The model includes installation costs, installation market, annual service costs and recurring service scenarios.

This pointer replaces the earlier standalone narrative to avoid maintaining duplicate numerical assumptions.
